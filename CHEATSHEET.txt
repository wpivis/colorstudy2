╔═══════════════════════════════════════════════════════════════╗
║                    🎨 COLORGORICAL CHEAT SHEET 🎨             ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│  QUICK START (Only 2 Terminals Needed!)                      │
└───────────────────────────────────────────────────────────────┘

  Terminal 1: Colorgorical (Start once, leave running)
  ┌─────────────────────────────────────────────────────────────┐
  │ $ cd /path/to/colorgorical                                  │
  │ $ python run.py --server --port 8888                        │
  │                                                             │
  │ ✓ Colorgorical running on http://127.0.0.1:8888/           │
  └─────────────────────────────────────────────────────────────┘

  Terminal 2: Everything Else (One command!)
  ┌─────────────────────────────────────────────────────────────┐
  │ $ cd /path/to/colorstudy2                                   │
  │ $ yarn dev                                                  │
  │                                                             │
  │ [PROXY] 🎨 Colorgorical Proxy Server running on port 3001  │
  │ [STUDY] VITE v7.1.7  ready in 186 ms                       │
  │ [STUDY] ➜  Local:   http://localhost:8080/                 │
  └─────────────────────────────────────────────────────────────┘

  🌐 Open: http://localhost:8080/
  
  To stop: Ctrl+C in Terminal 2 (stops both proxy and study)

┌───────────────────────────────────────────────────────────────┐
│  AVAILABLE COMMANDS                                           │
└───────────────────────────────────────────────────────────────┘

  yarn dev              ← 🌟 USE THIS! Starts proxy + study
  yarn serve            ← Study only (no Colorgorical)
  yarn proxy            ← Proxy only (for testing)

┌───────────────────────────────────────────────────────────────┐
│  TESTING                                                      │
└───────────────────────────────────────────────────────────────┘

  # Check if everything is working
  $ node test-colorgorical-integration.js

  # Quick health check
  $ curl http://localhost:3001/health

  # Test alternative generation
  $ curl -X POST http://localhost:3001/api/color-alternatives \
    -H "Content-Type: application/json" \
    -d '{"palette":["#58b5e1","#41d26b","#996f31"],"selectedIndex":1,"numCandidates":9}'

┌───────────────────────────────────────────────────────────────┐
│  TROUBLESHOOTING                                              │
└───────────────────────────────────────────────────────────────┘

  ❌ "Cannot connect to Colorgorical API"
     → Start Colorgorical: python run.py --server --port 8888

  ❌ Port 3001 already in use
     → Kill existing: lsof -i :3001 | grep LISTEN | awk '{print $2}' | xargs kill -9

  ❌ Want to skip Colorgorical?
     → Edit ColorPaletteComparison.tsx line 41: USE_COLORGORICAL = false
     → Then just: yarn serve

┌───────────────────────────────────────────────────────────────┐
│  SYSTEM ARCHITECTURE                                          │
└───────────────────────────────────────────────────────────────┘

  User Browser (http://localhost:8080)
        ↓
  Study Server (Port 8080) - Vite dev server
        ↓
  React Component clicks color
        ↓
  Proxy Server (Port 3001) - colorgorical-proxy.js
        ↓ Converts hex to LAB, extracts fixed colors
  Colorgorical API (Port 8888) - Python server
        ↓ Generates 9 optimized palettes
  Proxy extracts alternatives
        ↓
  React displays 9 alternatives

┌───────────────────────────────────────────────────────────────┐
│  PORTS IN USE                                                 │
└───────────────────────────────────────────────────────────────┘

  8888  → Colorgorical API (Python)
  3001  → Colorgorical Proxy (Node.js)
  8080  → Study Dev Server (Vite)

┌───────────────────────────────────────────────────────────────┐
│  FILES TO KNOW ABOUT                                          │
└───────────────────────────────────────────────────────────────┘

  QUICKSTART.md                    ← Start here!
  COLORGORICAL_SETUP.md            ← Detailed setup guide
  COLORGORICAL_INTEGRATION_SUMMARY.md  ← Technical overview
  
  colorgorical-proxy.js            ← Proxy server code
  test-colorgorical-integration.js ← Test script
  
  src/public/color-palette-study/assets/
    ColorPaletteComparison.tsx     ← React component with API calls

┌───────────────────────────────────────────────────────────────┐
│  DAILY WORKFLOW                                               │
└───────────────────────────────────────────────────────────────┘

  Morning:
    Terminal 1: python run.py --server --port 8888
    Terminal 2: yarn dev
  
  Development:
    Edit code → Vite hot reloads automatically
    Test in browser: http://localhost:8080/
  
  End of day:
    Terminal 2: Ctrl+C (stops proxy + study)
    Terminal 1: Ctrl+C (stops Colorgorical)

┌───────────────────────────────────────────────────────────────┐
│  CONFIGURING ALTERNATIVES                                     │
└───────────────────────────────────────────────────────────────┘

  Number of alternatives (default: 9):
    ColorPaletteComparison.tsx:
      numCandidates: 9  ← Change this

  Color generation weights:
    colorgorical-proxy.js line ~113:
      ciede2000: 1.0        ← Perceptual distance
      pairPreference: 0.0   ← Aesthetic preference

  Disable Colorgorical entirely:
    ColorPaletteComparison.tsx line 41:
      USE_COLORGORICAL = false

┌───────────────────────────────────────────────────────────────┐
│  USEFUL LINKS                                                 │
└───────────────────────────────────────────────────────────────┘

  Study:         http://localhost:8080/
  Proxy Health:  http://localhost:3001/health
  Colorgorical:  http://localhost:8888/

╔═══════════════════════════════════════════════════════════════╗
║  💡 TIP: Bookmark http://localhost:8080/ for quick access!    ║
╚═══════════════════════════════════════════════════════════════╝
